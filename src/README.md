# Source Notes

The current Sprint 0 runtime modules live in `/js` so `index.html` can open directly without a build process. Use `/src` for future source notes, prototypes, or optional non-runtime design files. If a build system is ever introduced, document the migration before moving runtime code.
